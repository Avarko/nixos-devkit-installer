Insert those personal private ssh keys and their public keys (.pub) here which you'll need in the devkit.
E.g. GitHub, ...

The keys will be later on added to ssh using Gnome Keychain.
It expects the keypair files to be named as <filename> and <filename>.pub
i.e. two files are considered to form a keypair by them having the same
base name.

Example:
my-github-devkit-id_ed25519
my-github-devkit-id_ed25519.pub
